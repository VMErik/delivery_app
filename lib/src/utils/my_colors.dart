// Este lo utilizaremos para los colores


import 'package:flutter/cupertino.dart';

class MyColors{
  // El 0xFF se tiene que especificar siempre, y despues va el codigo Hexadeciaml
  static Color primaryColor = Color(0xFF4D12AD);
  static Color primaryColorDark = Color(0xFF330F6E);
  static Color primaryOpacityColor = Color.fromRGBO(77, 18, 173, 0.09);
}